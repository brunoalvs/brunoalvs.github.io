<<<<<<< HEAD:backup/src/styles/main.styl
@import 'settings/colors';
@import 'tools/*';
@import 'generic/*';


body {
  background: $color1;
}

.home-video {
  position: fixed;
  display: block;
  min-width: 100%;
  min-height: 100%;
  z-index: 1;
  opacity: .2;

  &::-internal-media-controls-overlay-cast-button {
    display: none;
  }
}

header {
  position: relative;
  z-index: 9;
  height: auto;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;

  &.loading {
    position: absolute;
    transition: all 1s ease;
    height: 100vh;
  }

  &.loadDone {
    position: relative;
    transition: all 1s ease;
    height: 0vh;
    min-height: 100px;
  }

  .logo {
    padding: 1rem;
  }
}


section {
  position: relative;
  min-height: 300px;

  &.div1 {
    background: yellow;
  }

  &.div2 {
    background: blue;
  }
}
=======
@import 'settings/colors';
@import 'tools/*';

h1 {
  color: $white;
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
  font-size: 2rem;
  text-align: center;
}

body {
  background: linear-gradient(90deg, $gradient1 0%, $gradient2 100%);
  background-size: contain;
  height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
}
>>>>>>> parent of bf683e2... first tests:src/styles/main.styl
